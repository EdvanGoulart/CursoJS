//Função que retorna o maior valor entre dois números

function max( x, y){
    return x > y ? x : y; // Operação ternária.
}
//const max2 = (x,y) => x > y ? x : y; // Por Arrow Function
console.log(max(1,2));